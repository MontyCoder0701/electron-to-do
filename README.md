# electron-to-do

A simple to-do list app built with Electron

## Project setup

``` bash
npm install
```

## Run the app

``` bash
npm run start
```

## Build and package the app

``` bash
npx electron-builder build --mac
```
